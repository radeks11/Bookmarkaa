<UserControl x:Class="Bookmarkaa.BookmarkItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Bookmarkaa"
             mc:Ignorable="d" Background="#FF515151"
             Height="60" 
             d:DesignHeight="60" d:DesignWidth="640" Margin="4,4,4,4">

    <Grid>
        
            <Rectangle x:Name="Icon" Height="48" Width="48" Margin="10,0,0,0" HorizontalAlignment="Left" VerticalAlignment="Center">
                <Rectangle.Fill>
                    <ImageBrush ImageSource="{Binding IconBrush}"/>
                </Rectangle.Fill>
            </Rectangle>

            <Label x:Name="GroupName" Content="{Binding Name}" Margin="63,6,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" FontWeight="Bold" Height="26" />
            <Label x:Name="Folder" Content="{Binding Folder}" HorizontalAlignment="Left" Margin="63,30,0,0" VerticalAlignment="Top" Height="26" />
        
        </Grid>

</UserControl>
